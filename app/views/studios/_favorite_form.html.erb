<!-- log_in? でFavoriteForm表示の有無を決める-->
<div id="favorite_form">
  <% if Favorite.find_by(user_id: current_user.id, studio_id: @studio.id) %>
    <%= render "unfavorite" %>
  <% else %>
    <%= render "favorite" %>
  <% end %>
</div>