<% content_for(:html_title) { 'スタジオ編集' } %>

<div class="container new_studio_container">
  <h1>編集フォーム</h1>
  <% if current_user.id == @studio.created_user.id %>
    <%= render "form" %>
  <% else %>
    <%= render "limited_form" %>
  <% end %>
</div>